server {
    listen 80;
    listen [::]:80;
    server_name redirectHttps.extends.example.com *.redirectHttps.extends.example.com;
    location / {
        return 308 https://$server_name$request_uri;
    }
}
server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name redirectHttps.extends.example.com *.redirectHttps.extends.example.com;
    ssl_certificate cert/host-redirectHttps.extends.example.com-c0a95112.crt;
    ssl_certificate_key cert/host-redirectHttps.extends.example.com-c0a95112.key;
    expires 1y;
    add_header InheritRoot0 true;
    add_header DefaultReplace -1;
    location / {
        alias /usr/local/www/lfs-static;
    }
}
